# AI Coach Frontend

Фронтенд приложение для AI помощника на Next.js, React и Tailwind CSS.

## Особенности

- 🎨 Минималистичный дизайн с темной темой
- 🔴 Неоновые красные акценты
- 📱 Адаптивный интерфейс
- 🔐 Аутентификация пользователей
- 💬 Чат интерфейс с AI помощником
- 📝 Управление чатами (создание, удаление)

## Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Axios** - HTTP клиент

## Установка

```bash
cd frontend
npm install
```

## Настройка

Создайте файл `.env.local` в корне проекта `frontend`:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## Запуск

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## Структура проекта

```
frontend/
├── app/                    # Next.js App Router
│   ├── login/             # Страница входа
│   ├── register/          # Страница регистрации
│   ├── chats/             # Страница списка чатов
│   └── chats/[chatId]/    # Страница чата
├── components/             # React компоненты
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── Message.tsx
│   └── ChatCard.tsx
├── contexts/               # React контексты
│   └── AuthContext.tsx
└── lib/                   # Утилиты
    └── api.ts             # API клиент
```

## API Endpoints

Все эндпоинты покрыты фронтендом:

### Аутентификация
- `POST /api/user/register` - Регистрация
- `POST /api/user/login` - Вход

### Чаты
- `GET /api/chat/all` - Список чатов
- `GET /api/chat/{chatId}` - Получить чат
- `POST /api/chat` - Создать чат
- `DELETE /api/chat/{chatId}` - Удалить чат
- `PATCH /api/chat/{chatId}/title` - Обновить заголовок

### Агент
- `POST /api/agent/{chatId}` - Отправить сообщение
- `GET /api/agent/{chatId}/all` - Все сообщения чата
- `GET /api/agent/documents` - Количество документов

# Фронтенд для AI Coach Assistant

## ✅ Что создано

Полнофункциональный фронтенд на **Next.js 14**, **React 18**, **TypeScript** и **Tailwind CSS** с минималистичным дизайном в темной теме с неоновыми красными акцентами.

## 🎨 Дизайн

- **Темная тема**: Черный фон (#0a0a0a) с темными поверхностями
- **Неоновые акценты**: Красный цвет (#ff006e) с эффектом свечения
- **Минималистичный UI**: Чистый и современный интерфейс
- **Анимации**: Плавные переходы и эффекты свечения

## 📱 Покрытие всех эндпоинтов

### ✅ Аутентификация
- [x] `POST /api/user/register` - Страница регистрации
- [x] `POST /api/user/login` - Страница входа

### ✅ Чаты
- [x] `GET /api/chat/all` - Список всех чатов
- [x] `GET /api/chat/{chatId}` - Получение чата
- [x] `POST /api/chat` - Создание нового чата
- [x] `DELETE /api/chat/{chatId}` - Удаление чата
- [x] `PATCH /api/chat/{chatId}/title` - Обновление заголовка

### ✅ Агент
- [x] `POST /api/agent/{chatId}` - Отправка сообщения агенту
- [x] `GET /api/agent/{chatId}/all` - Получение всех сообщений чата
- [x] `GET /api/agent/documents` - Количество документов в базе

## 📂 Структура проекта

```
frontend/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # Главный layout с AuthProvider
│   ├── page.tsx                 # Главная страница (редирект)
│   ├── globals.css              # Глобальные стили
│   ├── login/
│   │   └── page.tsx            # Страница входа
│   ├── register/
│   │   └── page.tsx            # Страница регистрации
│   └── chats/
│       ├── page.tsx            # Список чатов
│       └── [chatId]/
│           └── page.tsx        # Интерфейс чата
├── components/                   # React компоненты
│   ├── Button.tsx              # Кнопка с неоновым эффектом
│   ├── Input.tsx                # Поле ввода
│   ├── Message.tsx              # Компонент сообщения
│   └── ChatCard.tsx             # Карточка чата
├── contexts/                     # React контексты
│   └── AuthContext.tsx         # Контекст аутентификации
├── lib/                         # Утилиты
│   └── api.ts                  # API клиент (все эндпоинты)
├── tailwind.config.ts           # Конфигурация Tailwind
├── tsconfig.json                # Конфигурация TypeScript
└── package.json                 # Зависимости
```

## 🚀 Быстрый старт

1. **Установите зависимости:**
```bash
cd frontend
npm install
```

2. **Создайте `.env.local`:**
```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

3. **Запустите бэкенд** (убедитесь, что он работает на порту 8000)

4. **Запустите фронтенд:**
```bash
npm run dev
```

5. **Откройте браузер:** [http://localhost:3000](http://localhost:3000)

## 🎯 Основные функции

### Аутентификация
- Регистрация нового пользователя
- Вход в систему
- Автоматическое сохранение токена
- Защита маршрутов

### Управление чатами
- Просмотр списка всех чатов
- Создание нового чата
- Удаление чата
- Автоматический выбор первого чата

### Чат интерфейс
- Отправка сообщений агенту
- Отображение истории сообщений
- Автоматическая прокрутка к новым сообщениям
- Индикатор загрузки
- Отображение количества документов в базе

## 🎨 Особенности дизайна

### Цветовая схема
- **Фон**: `#0a0a0a` (темный)
- **Поверхности**: `#1a1a1a`, `#2a2a2a`
- **Акценты**: `#ff006e` (неоновый красный)
- **Текст**: Белый и серые оттенки

### Эффекты
- Неоновое свечение на кнопках и акцентах
- Плавные анимации переходов
- Кастомный скроллбар с красным цветом
- Hover эффекты на интерактивных элементах

## 📝 Компоненты

### Button
- Варианты: primary (неоновый красный), secondary, danger
- Состояние загрузки
- Эффекты свечения

### Input
- Темная тема
- Фокус с неоновым эффектом
- Обработка ошибок

### Message
- Разные стили для пользователя и AI
- Неоновый красный для сообщений пользователя
- Темная тема для ответов AI

### ChatCard
- Карточка чата в списке
- Выделение выбранного чата
- Кнопка удаления

## 🔒 Безопасность

- Токен хранится в localStorage
- Автоматическое добавление токена к запросам
- Редирект на страницу входа при 401 ошибке
- Защита маршрутов через проверку аутентификации

## 📦 Зависимости

- `next` - Next.js фреймворк
- `react` - React библиотека
- `react-dom` - React DOM
- `axios` - HTTP клиент
- `tailwindcss` - CSS фреймворк
- `typescript` - Типизация

## 🎉 Готово к использованию!

Все эндпоинты бэкенда покрыты фронтендом. Приложение готово к использованию!

